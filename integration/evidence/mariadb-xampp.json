{
  "profile": "mariadb-xampp",
  "status": "not-tested",
  "observedAt": "2026-08-02",
  "runtime": "docker.io/library/mariadb:11.4.12",
  "commands": [
    "docker compose --project-name m3verify --profile mariadb -f compose.dev.yml config --quiet",
    "docker compose --project-name m3verify --profile mariadb -f compose.dev.yml up --build --abort-on-container-exit --exit-code-from migrate-mariadb migrate-mariadb"
  ],
  "observations": [
    {
      "check": "compose configuration",
      "exitCode": 0
    },
    {
      "check": "clean Docker MariaDB migration parity",
      "exitCode": 0,
      "result": "migration completed"
    },
    {
      "check": "native XAMPP MariaDB dedicated test database",
      "result": "not tested; no database reset was requested"
    }
  ],
  "scope": "Docker MariaDB parity passed; this evidence deliberately does not claim native XAMPP or Oracle MySQL support."
}
